# GitHub Pages Production Environment Template
# Copy this to .env.production and update with your actual values

# API configuration - Update these with your actual backend URLs
VITE_API_URL=https://your-backend-api.com/api
VITE_API_VERSION=v1

# WebSocket configuration - Update for production
VITE_REVERB_HOST=your-backend-api.com
VITE_REVERB_PORT=443
VITE_REVERB_SCHEME=https
VITE_PUSHER_APP_KEY=your-production-key

# GitHub Pages specific (auto-configured)
VITE_BASE_URL=/meeting-audio-studio/

# Demo mode - set to true to use mock data instead of API calls
VITE_DEMO_MODE=true

# Instructions:
# 1. Copy this file to .env.production
# 2. Update the API URLs with your actual backend
# 3. Set VITE_DEMO_MODE=false if you have a working backend
# 4. For GitHub Actions, these values can be set as repository secrets
